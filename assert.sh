
########################ASSERT################################
curl -V
tree --version
npm -v
cargo -V
go version
pip --version
pipx --version
git --version
ssh -V
docker --version
code --version | head -n 1
zsh --version
make --version
opencode --version
nvim --version | head -n 1
tmux -V 
# taproom --version
superfile --version
branchlet --version
pkgtop -v
stormy --version
smassh --version 
jrnl --version 
# navi --version
ls ~/.local/bin/edex-ui
kubectl version --client
kind version
